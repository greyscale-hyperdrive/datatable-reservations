const chai = require('chai');
const chaiHTTP = require('chai-http');
const server = require('../server');

chai.use(chaiHTTP);

describe('CRUD Endpoints for Reservations' () => {

  it('should add a restaurant to the system on a POST request to the /restaurants endpoint', (done) => {

  });


});
